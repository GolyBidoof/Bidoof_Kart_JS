var karts;

class KartClass {
    constructor(maxSpeed, acc, dec, handling) {
        this.maxSpeed = maxSpeed;
        this.acc = acc;
        this.dec = dec;
        this.handling = handling;
    }
}
